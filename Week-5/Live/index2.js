Object.prototype.chai = function () {
  console.log("Chai");
};

const arr = [1, 2, 3];
arr.push(4);

const str = "Swarnavo";
str.toUpperCase();

const obj = { x: 1 };
obj.chai();

// Dot operator objects ke properties ko access karne meh help karta hai
